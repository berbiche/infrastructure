apiVersion: v1
kind: Service
metadata:
  name: sonarr
  labels:
    app.kubernetes.io/name: sonarr
spec:
  ports:
    - name: http
      protocol: TCP
      targetPort: 8989
      port: 80
  selector:
    app.kubernetes.io/name: sonarr
  type: ClusterIP
