apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jackett-config
  labels:
    app.kubernetes.io/name: jackett
spec:
  storageClassName: openebs-sc-iscsi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
